# aac-forge environment configuration

# LikeC4
# No special variables needed — CLI works out of the box

# Optional: LLM provider API keys (for adapter scripts, not required for model validation)
# DEEPSEEK_API_KEY=
# GEMINI_API_KEY=
# ANTHROPIC_API_KEY=
